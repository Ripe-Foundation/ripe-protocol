# Robinhood agent handoff compatibility route

This path is retained so existing links do not break.

The sole canonical deployment-owner and deployment-agent handoff is
[`deployment-owner-quickstart.md`](deployment-owner-quickstart.md). Start
there and follow its phase-specific links only when the named gate is reached.

Machine-readable current status remains in [`status.yaml`](status.yaml).
This compatibility page is not an independent source of lifecycle facts,
counts, blockers, hashes, commands, or deployment instructions.
