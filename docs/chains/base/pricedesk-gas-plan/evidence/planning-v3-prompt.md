Address the reported failure: at the reviewer's reference state, the proposed Base PriceDesk returns zero for `getPrice(undyUSDC)` while the active desk returns a price. Reproduce that claim, then prepare the narrowest qualified change and separately reviewable gas-budget hardening. Minimize contract changes; 3M/3M is experimental. Pricing-policy changes are analysis-only and outside implementation scope for this run. No live deployment, Safe signing/submission/execution, push, PR publication, or merge is authorized.

The reviewer reported approximately **$40.6k of undyUSDC exposure**; its valuation time/method were not supplied here, so it is historical context, not a current balance or loss estimate. Refresh relevant positions and token units in Phase 0. The source-bound operational gate is PR #231's **Stage 3 D01**: due snapshots silently skipped through caller underfunding are not accepted. Quote parity alone cannot clear that gate. Cost drift with Underscore vault state is a hypothesis to test across pins and stressed valid states, not a demonstrated explanation for the original failure.

Execute Phases 0–6 in order. Finish the narrow workstream's qualification attempt before starting the hardening prototype. If the narrow candidate cannot qualify, write a specific NO-GO/partial result rather than wait or weaken its criteria; then investigate whether Phase 5 can resolve it. If only one workstream can be finished, prioritize the narrow one. A NO-GO report completes an investigation, not the requested implementation. The detailed requirements A–F below are the execution reference.

All phase outputs below live under `docs/chains/base/pricedesk-gas/` in the selected implementation checkout. This planning packet lives separately under `docs/chains/base/pricedesk-gas-plan/`; copy the whole packet into an isolated checkout because it is not present at the pinned implementation commit. Preserve its files as documentation-only inputs and hash them separately.

| Phase | Action and reference | Exit criterion | Required output |
| --- | --- | --- | --- |
| 0 — Bind | Verify the pinned SHA, isolated checkout, live identities, Safe state, exposure and artifact hashes (A, E). | Every material identity is pinned or explicitly unknown; no unknown is silently used. | `binding.json`, `decision-log.md` |
| 1 — Reproduce and screen | Reproduce the old-cap failure and apply the source-count/transaction-envelope arithmetic before optimizing (C). | Reproduction or a documented discrepancy, plus PASS/REJECT/UNPROVEN envelope status. | `reproduction.json`, `budget-envelope.json` |
| 2 — Sweep | Independent quote/snapshot cap sweeps, cold boundary checks, state-drift cases and complete-transaction probes (C, E). | Every required case has a correctness result, gas boundary and provenance, or an explicit blocked result. | `qualification.json`, `evidence/` |
| 3 — Decide | Apply the preference order and headroom floors (D). | One selected in-scope option, or a concrete NO-GO; reasons recorded before editing production code. | `decision-log.md` |
| 4 — Narrow implementation | Implement only the selected option, run its regression/full-transaction checks, and classify D01 status (D, E). | Qualified candidate, or explicit partial/NO-GO with remaining blockers; no broad hardening detour. | `narrow-result.md`, local narrow commits if code changed |
| 5 — Hardening | Run the bounded static-context prototype matrix, then implement hardening only if it passes (B, D). | All matrix rows and the control-flow argument pass, or a bounded feasibility failure with smallest next-step proposal. | `hardening-matrix.json`, `hardening-result.md`, separate local commits if feasible |
| 6 — Handoff | Recheck live drift, freeze source/build/config identities, compile final evidence and draft activation instructions (F). | Reviewer can distinguish each workstream's completed, blocked and owner-held items and reproduce every claimed pass. | `README.md`, `activation-handoff.md`, final `qualification.json` |

Reserve the first pass for identity, reproduction and arithmetic; do not tune setters before the sweeps. The hardening prototype has a specific two-approach/four-active-hour bound in B. Repair harness errors before attributing them to protocol design. Write blocked/no-go evidence and continue independent work. Pricing-policy proposals never pause this run: quantify them and leave them for a separate owner decision. Surface material live-state drift both in `decision-log.md` and in the current task's progress message; send no messages to third parties.

---

A. **Source, scope, and live-state reference.**

   Repository: `$RIPE_REPOSITORY`. PR #231 is open against `master`. Its head was independently verified on 2026-09-19 as `bdf7f3da7113aabde60ab8eceab6a960a841bb88`, branch `codex/base-upgrade-fork-rehearsal`. The local `master` reference was `1ede38351e6da918806dff70d34b2066bf8cde8c`. Re-verify before starting; these are dated references.

   Use an isolated worktree and a new `codex/pricedesk-base-gas-qualification` branch starting from the full PR head above. Do not switch, reset, clean, or modify the coworker's checkout/branch or unrelated untracked files. Recheck the remote head first. If it moved, record the new SHA/delta in `decision-log.md` and continue against the frozen full SHA in this prompt. Do not silently rebase or wait for a target decision. Flag integration reconciliation as outstanding; final deployment qualification must later bind the reconciled tree.

   The owner selected a **stack on PR #231** for this handoff. The narrow branch is based on the pinned #231 head and its eventual PR targets `codex/base-upgrade-fork-rehearsal`, not `master`. Hardening uses `codex/pricedesk-gas-hardening`, based on the frozen narrow-result commit, with its eventual PR targeting `codex/pricedesk-base-gas-qualification`. If narrow work made no code changes, record its no-change result commit and retain the same lineage. Publish nothing in this run. Do not independently land a duplicate #231 diff on master or edit the coworker's branch.

   Later integration order is narrow into #231's branch, then #231 into master; hardening is reconciled with that lineage and retargeted only after its dependency lands. Before any later deployment, freeze the **actual integrated commit/tree** containing exactly the chosen changes, compiler inputs and constructor/configuration manifest. Rebuild and refresh affected qualification after any tree-changing merge/rebase/cherry-pick. Draft Safe instructions must cite that build identity, not a branch name or the pre-fix #231 SHA. Finish this run with local, reviewable commits/reports only.

   For a clean frozen baseline, `git worktree add --detach <unused-writable-path> bdf7f3da7113aabde60ab8eceab6a960a841bb88` is appropriate; create the named working branch in a separate isolated checkout. If linked-worktree creation is denied on `.git/worktrees`, report the exact error once. Use an allowed standalone `git clone --no-hardlinks --no-checkout $RIPE_REPOSITORY <unused-writable-path>` and check out the pinned SHA there; this avoids modifying shared worktree administration. If Git metadata creation is also disallowed, export the pinned tree with `git archive` for analysis and deliver patches/diffs, with local commits explicitly blocked. Do not change permissions, clean another checkout, or retry a policy-denied write through a different spelling.

   The main implementation is `contracts/registries/PriceDesk.vy`. The PR changes the 250k quote / 150k snapshot constants into public immutables, adds two constructor arguments, and asserts both nonzero. The 75k feed-check constant is unchanged. The candidate at `0xad70893e2f51076b0e9ba18fc593e924593a073f` was read on-chain with 1.5M quote / 1.5M snapshot getters and 17,898 bytes of runtime. Authenticate its complete source/build/constructor identity before using it as evidence; matching getters or size alone is insufficient.

   Read the pinned branch's `docs/chains/base/staged-upgrade-deployment.md`, `docs/chains/base/review-231.md`, and bridge migration `migrations/base-mainnet/2026091403_StageBaseBridgePriceDesk.py`. Preserve their existing Stage 2 recovery, empty-MissionControl, and Stage 3 D01 gates. The bridge retains existing live source state; Stage 3's desk with unconfigured replacement sources is a different candidate. Do not conflate them.

   Entry and handoff checks must independently read Base chain ID 8453, block/hash/time, RipeHq code identity, active `getAddr(7)`, `pendingAddrUpdate(7)`, governance, registry delay, and the relevant Safe queue. Resolve live departments via authenticated RipeHq getters, not unverified manifest labels.

   Reference readback at Base block **51,524,517**, hash `0x37ebbf61930e433ee35894adda51ab9054cff823e5febe9c6ef32c589606f10b` (2026-09-19 17:19:41 UTC): RipeHq `0x6162df1b329E157479F8f1407E888260E0EC3d2b`; governance Safe `0xe488a42D33b3Af5d3E5Cd5680938d8369716D1bf`; active desk `0x2F7901BE53cC94AEF174f1a0764430840360Ef53`; slot-7 pending update empty; registry delay 21,600 blocks. Safe nonce 477 remained unexecuted. Its 22-call batch, Safe transaction hash `0x6422e6d187b9f0abfe3b11a51fadb828f57207c0b9e53f260e0992df5746636e`, contains `startAddressUpdateToRegistry(7, 0xad70893e2f51076b0e9ba18fc593e924593a073f)`. Refresh these facts; this is a snapshot, not a continuing monitor.

   Treat that queued call as a specific unresolved activation hazard. Executing the batch would propose the candidate; it would not itself confirm slot 7. The inspected RipeHq wrapper restricts confirmation to governance, so do not describe confirmation as permissionless. Verify the deployed authorization surface. Distinguish an unexecuted Safe proposal, an on-chain pending entry, an executable entry, and an activated desk. Record changes immediately in `decision-log.md` and the current task progress message, without taking live corrective action.

B. **Gas semantics and hardening feasibility.**

   A root pricing request is a PriceDesk operation outside a demonstrated budget-scoped callback from a source currently being evaluated. It includes protocol/user calls and source-originated standalone operations. `qualifyCallerPriceSource` must enforce its own root admission guarantee even when its caller is already registered.

   Root caller underfunding means the PriceDesk cannot supply a required source-call budget and preserve the proven continuation reserve because the enclosing caller supplied insufficient gas. Detect it before a reduced call where possible, and revert atomically rather than returning an ordinary zero/no-feed/no-op. This applies to non-strict entry points too. Very low gas may produce an ordinary transaction OOG rather than a custom error; it must not produce a successful misleading result.

   Parent-budget exhaustion is different: once an outer source receives its full configured allowance, its computation and nested dependency traversal must fit inside that allowance. A nested attempt may receive less than its nominal standalone ceiling without proving root caller underfunding. Failure within this fully supplied source envelope remains isolatable source failure. Allow healthy outer fallback when aggregate gas permits. Do not require another full global allowance at every nested frame, and do not promise a positive result for every genuinely failing source.

   This distinction is the semantic repair, not proof that a particular implementation can enforce it. The quote/conversion/qualification path is `@view`; source calls are static. No persistent or transient depth-counter writes, reentrancy-style write flags, or mutable shared budget accounting may be introduced on that path. STATICCALL restrictions propagate into descendants. Snapshot entry is stateful, but any static descendants retain their restrictions. See [EIP-214](https://eips.ethereum.org/EIPS/eip-214) and [EIP-1153](https://eips.ethereum.org/EIPS/eip-1153).

   Run this feasibility work in Phase 5 after the narrow result is recorded. The semantic root/nested definition above is a desired contract, not an available runtime detector. Under the current static ABI, an unknown context creates a dilemma: treating it as root may reject legitimate nested calls; treating it as nested can preserve the original silent-underfunding defect. Neither outcome qualifies for production. For the **prototype only**, unknown means root and receives the strict check. Measure any resulting nested breakage against a sufficiently funded positive control; do not assume breakage is inevitable if per-source envelopes make the route fit.

   Bound the prototype to **at most two explicitly described candidate approaches and four hours of active engineering effort**, whichever limit comes first; RPC/test wait time is reported separately. Approach one is the fail-closed unknown-to-root baseline. A second approach must remain within the stated static/file/ABI scope and have a written reason it can resolve an observed failure. One harness-correct deterministic contradiction closes that approach. At the bound, deliver the matrix, traces and smallest interface/scope proposal; do not begin a third approach or broaden production edits. This is a planning effort limit, not permission to declare an unproved design complete.

   Prototype acceptance requires all rows below plus a source/compiled-control-flow explanation of which entry rule applies at every call boundary. Tests alone do not establish an unobservable context distinction. A row skipped, simulated through the wrong desk, or dependent on an unsupported caller assumption is not a pass.

   | Case | Required observation |
   | --- | --- |
   | Ordinary protocol/user root, strict and non-strict | Enough gas preserves the expected result; insufficient required call funding reverts or OOGs atomically, never succeeds with a substituted zero/no-feed. |
   | Registered source making a standalone call | Full root guarantee applies despite registry membership. |
   | True callback within a fully funded source envelope | Expected supported nested prices/fallbacks work without requiring another impossible full global allowance. |
   | Unregistered candidate callback | Admission's nested route is executable under its intended allowance, without a registration-based false root classification. |
   | Registered/unregistered source qualification and re-qualification | Direct admission gets the full effective source budget; aggregate fallback cannot hide its failure. |
   | Earlier exhausted sources and remaining-gas boundary | Healthy fallback succeeds inside the qualified envelope; aggregate underfunding is explicit. |
   | Source revert, malformed response, genuine envelope exhaustion | Isolation and strict/non-strict policy remain correct; spoofed error bytes do not gain authority. |
   | Removed/replaced/re-registered or adversarial source | Classification/override lifecycle does not weaken root guarantees or inherit an obsolete source's allowance. |
   | Feed checks and due/not-due snapshots | Due+sufficient updates state; detected starvation reverts previous writes; genuine failures and legitimate no-ops remain distinguishable. |
   | Configuration/maximum envelope and cyclic/deep route cases | Boundaries are enforced; unsupported cycles/depth fail safely and cannot qualify a broken required route. |

   Registry membership is not proof of an active callback: registered sources also call PriceDesk during standalone validation, and a candidate can need callbacks before registration. A membership-based rule may be investigated, but it is not an approved solution or a loophole in the underfunding guarantee. Test registered standalone calls, true callbacks, unregistered candidate callbacks, re-qualification, removed/replaced sources, and adversarial registered sources. Force admission to the full effective quote budget regardless of membership.

   Prove that classification cannot silently weaken root behavior and that nested failure handling preserves source isolation. Trace failure propagation across non-reverting `raw_call`; source-provided revert bytes are not authoritative evidence of origin. Explicitly state which errors propagate and which remain source-local. If the root/nested contract cannot be proved inside the allowed scope, mark hardening blocked on a concrete design decision and give the smallest interface/caller change proposal with reproduction evidence. Do not ship an exemption under a different name.

   Ordinary correctly funded source reverts, malformed responses, and allowance exhaustion must preserve intended fallback. Preserve `_shouldRaise`/`mustRaiseOnZero` semantics for genuine source unavailability. Keep the retired `_staleTime != 0` behavior: strict rejection or non-strict zero is intentional.

C. **Measurement protocol and transaction-envelope gate.**

   Start the failure investigation at **Base block 51,493,600**, supplied by the reviewer as an approximate reference. Retrieve and record its hash, candidate deployment availability, source states, and exact original reproduction if available. The branch separately reports bridge quote parity at **51,491,284**; that is an earlier comparison case, not proof of a later regression's cause. If the failure does not reproduce, investigate block/state, bytecode, caller gas, warming, and callback routing. Report the discrepancy and seek the original trace when needed. Do not conclude there is no defect, and do not assume the block must be the only thing wrong.

   Run historical reproduction separately from qualification at a fresh finalized block. Do not overwrite historical artifacts or silently move their pins. Record every fork time/block advance and state mutation, including changes made to make snapshots due.

   For the immutable-generation experiment, compile the exact pinned source once and redeploy a fresh fork-local desk for each constructor pair. Reconstruct identical registry/configuration/token-scale state from the same fork baseline. Do not implement storage setters first merely to run the sweep. Label later storage-version results separately and remeasure their lookup/guard overhead.

   Use the master **250k quote / 150k snapshot / 75k feed-check** generation as an explicitly different baseline. For the PR generation, independently sweep quote caps **1.5M, 2M, 3M, 6M**, holding snapshot allowance fixed; then sweep snapshot caps **150k, 1.5M, 2M, 3M, 6M**, holding the qualified quote allowance fixed. Include a combined 3M/3M trial and the final selected pair. These are experiments, not approved minima/maxima. Add source-specific experiments if a uniform cap changes unrelated failing-source costs.

   Measure both gas consumed and the minimum successful caller limit. Bracket failure/success and bisect call gas on an identical restored state for each trial; success means the expected price or due snapshot-state transition, not simply a successful receipt. Probe around the resulting boundary and at higher limits. If fallback selection or gas-dependent behavior makes success non-monotonic, map the bands and report that limitation instead of treating bisection as a global proof.

   Trace requested/forwarded/consumed gas, source identities, answering source, failure causes, traversal order, and nested frames. Determine whether requirements plateau or increase with allowances; do not assume the historical 1,719,201 figure is invariant or assign it to a call frame without its original evidence. Compare relevant historical/fresh pins and valid stress states for Underscore vault growth, nested dependencies and full snapshot rings. This explicitly tests the hypothesis that cost changes as vault state changes; it does not assume that hypothesis explains the original difference.

   **Callback routing is a measurement validity gate.** Some source methods accept a desk argument; other validation paths resolve RipeHq's canonical desk, and some enforce that identity. On the fork, bind the intended desk through the actual relevant paths and trace every PriceDesk address. Repoint fork-local RipeHq where required, under a clearly recorded fixture. Never repoint live HQ. A benchmark that prices dependencies through the old desk does not qualify the replacement.

   Build the actual deployed source/dependency graph, including source-originated validation and quote callbacks. Do not assume all ten source files recurse: current Chainlink/Pyth/Stork do not contain direct `PriceDesk.getPrice` calls. Distinguish available implementations, allocated slots, disabled slots, registered sources, and executable routes. The bridge expects allocated IDs 1–9, exclusive `numAddrs() == 10`, disabled slot 3, and retained legacy Aero at 6: that is **eight enabled addresses**, not nine, if the pinned inventory still matches. Verify each slot and priority traversal. Curve and wrapper routes need special attention; PriceDesk has no explicit depth guard.

   Enumerate all executable pricing/conversion callers at the selected commit and deployed generations, including aliases, helpers, strictness, and user-action consequence. The review's “84 sites across 22 files” is a lead to verify, not an established count. Do not count interfaces as executable calls.

   Independently trace snapshots through external vault/pool dependencies. Source-level snapshot methods may be no-ops or lack direct PriceDesk calls, but that does not prove the complete external graph cannot call back; Undy snapshots call the vault's `convertToAssets`. Due updates must change the appropriate ring/cursor/timestamp; legitimate same-block/not-due False results remain valid. Teller ignores the snapshot boolean, so receipts alone cannot establish an update.

   Qualify full transactions against the proposed department stack and retained unmigrated vaults: deposits/withdrawals including undyUSDC, borrowing, collateral valuation, liquidation, deleveraging, snapshot-producing flows, multi-asset positions, and keeper batches. Measure actual Base constraints, RPC estimation, intrinsic versus execution gas, wallet/keeper buffers, and worst-case fallback headroom. The branch records an ordinary-transaction ceiling of 16,777,216; verify the applicable Base rules at the qualification pin. Do not use Robinhood's 32M envelope. Above-limit diagnostic calls, if useful, must be labeled non-submittable.

   Perform this arithmetic screen in Phase 1, before expensive optimization. At eight enabled sources, eight full 3M quote envelopes total **24M**, above 16,777,216 before desk/transaction overhead. Seven earlier exhausted 3M sources alone consume 21M before a healthy last fallback. A uniform-3M configuration therefore cannot claim that full-traversal isolation guarantee at this ceiling; keep it only as a diagnostic sweep point unless a materially tighter reachable bound is proved. The review's nine-source/27M count included the disabled slot.

   Avoid double counting: nested execution is paid from its outer source's allowance; it is not added on top of that same cap. The outer cap must still be enough for its internal dependency graph, EIP-150 forwarding and completion. Repeated root quote operations, separate feed checks, separate snapshot calls, and other transaction work are additional costs. Snapshot traversal is separately bounded by the calls it can actually perform; do not assume every source has a due snapshot or that failing sources are free.

   For each admitted configuration/topology and supported batch size, derive a conservative bound on the **minimum outer gas limit sufficient for the required worst-case behavior**, including intrinsic gas, non-pricing work, reachable call counts, per-source overrides, nested funding feasibility, call/return overhead and continuation reserves. Require that bound to be at most `floor(0.80 * T)`, where `T` is the applicable per-transaction ceiling. At `T = 16,777,216`, this ceiling is **13,421,772**, leaving at least 20% of T unused. Sum disjoint outer call envelopes only once. Validate the bound with exhausting-source mocks and real final-stack transactions. Label loose bounds UNPROVEN, not an observed failure; an explicit reachable lower bound above T is a rejection.

   Validate the maximum combination admitted by the chosen defaults/overrides at the fixed qualified source inventory, and invalidate qualification on source-code/topology changes. Budget setters and any admission checks must not silently admit combinations outside a claimed enforced envelope. If enforcing the required envelope needs out-of-scope registry/caller policy, report that scope decision. Do not lower headroom, ignore expensive fallback cases, assume a failing source returns quickly, or imply universal liveness under arbitrary future source additions to make a configuration pass.

D. **Selection policy, implementation limits, and parameters.**

   Write the option decision and supporting evidence in Phase 3, before implementation. Use this preference order among options that satisfy the same required behavior: (1) retain the active desk only if it is compatible with the proposed stack and meets this task's required isolation/D01 gates; (2) a configuration-only change that demonstrably preserves pricing policy and passes the envelope; (3) the smallest in-scope replacement, preferring differentiated per-source budgets when a uniform cap cannot fit; (4) broader hardening after the narrow result. If no in-scope option passes, deliver NO-GO and a concrete follow-up proposal. Keeping an incompatible or uncapped active desk is a baseline/defer-activation option, not a qualified fix. Uncapped pricing is a diagnostic comparison, not permission to discard isolation. Pricing-policy changes never enter this run's implementation ranking; they are proposals only.

   Default qualification floors: each chosen source allowance must be at least **1.50 times** its worst measured cold minimum-required forwarded allowance over the supported valid states/routes, rounded up; the whole-operation bound must also pass C's **20%-of-T reserve**. Re-evaluate the measurement at the selected cap/override configuration because nested requirements can move with it. A measured 2M stable requirement suggests at least 3M for that source, not a global 3M allowance for all sources. Use larger headroom if measured state growth/variance warrants it. These are conservative engineering acceptance floors for this work, not a theorem that unbounded future vault growth is safe. Record the supported state envelope and requalification triggers. Do not reduce either floor to force a pass; report a scope/configuration decision instead. Select the smallest bound-compliant cap above the floor and rerun the final configuration.

   Test lower Undy snapshot counts and source-priority changes on the fork. Both are analysis-only if they alter pricing policy; no pricing-policy change is implemented in this run. Changing `maxNumSnapshots` resets/reseeds the ring; changing the other inspected configuration fields preserves its cursor/progress. A valid fresh lone snapshot can keep PPS available, but historical smoothing is lost and rebuilds over time/observations. A smaller ring can also alter the eventual averaging window. Measure price discontinuity, reduced smoothing/manipulation resistance, `maxUpsideDeviation` interaction, and recovery. Reset asserts a valid newly obtained snapshot, so confirmation may revert. The Undy update itself is a timelocked propose/confirm lifecycle. Verify these behaviors on the actual deployed source generation.

   Global source-order changes may change the winning oracle for unrelated assets. Compare prices, authority, and fallback across the affected inventory. Preserve current policy throughout this run. Quantify any proposed smoothing/history or winning-oracle change and deliver its concrete parameters, impact and evidence for a separate owner decision; do not pause this run awaiting that decision.

   Hardening parameters belong in local PriceDesk storage, not MissionControl/`PriceConfig`. Make quote and snapshot budgets independently readable and adjustable. Evaluate global defaults with address-keyed overrides using the inventory; if selected, define unset/reset behavior and test removal, replacement, re-registration, and changed code identity. Do not let a registry-ID budget silently transfer to a new contract.

   Keep feed checks at 75,000 gas for this scope and test their underfunding boundary. A contradiction from actual deployed-source measurements is a scope/design decision to report, not permission to silently raise this cap.

   Authorize budget/reserve changes using `gov._canGovern` alone, not the pause-sensitive `_canPerformAction`. Setters must remain callable while PriceDesk is paused. Verify HQ governance works after local governance is relinquished, and reject arbitrary protocol contracts. Proposed policy: immediate governance updates, with no additional gas-parameter timelock, because this is a recovery control. Document this choice distinctly from existing registry/source timelocks and flag any conflict with owner policy before changing it.

   If the design uses a continuation reserve, make it separately readable and governance-adjustable within proved nonzero lower and finite upper bounds. Apply justified bounds at initialization and every update; emit old/new values. Recoverability is not a substitute for a safe lower bound. Demonstrate invalid/small/large settings cannot silently disable underfunding protection. Add a configurable minimum-forwarding parameter only if the design actually needs it and can specify its semantics.

   Use the same effective quote budget for ordinary source execution and admission. Model pre-call overhead, cold access, memory, EIP-150 rounding, return-data handling, and continuation work against the compiled path. A numerical formula using `msg.gas` without accounting for intervening instructions is not sufficient. Bound total traversal and qualify the maximum admitted configuration together with source count, nesting, overrides, and batch limits. Nested work fits inside each outer cap; the complete transaction model adds disjoint operations, overhead and funding requirements as specified in C rather than counting nested gas twice. [EIP-150 specifies forwarding from the parent's remaining gas](https://eips.ethereum.org/EIPS/eip-150).

   Allowed production changes: PriceDesk, the necessary PriceDesk ABI/interface declarations, its constructor/configuration/migration wiring, and substantive tests/reports. Do not change price arithmetic, freshness, `PriceConfig`, MissionControl logic, Teller, other department contracts, source implementations/interfaces used by sources, vault logic, activation policy, funds/positions, or unrelated upgrade remediation as incidental fixes. Source/department interface changes, registry-policy expansion, or an all-source callback redesign require an explicit owner scope decision before production edits. Prepare a concrete proposal and prototype evidence, then stop the dependent hardening work. Do not abandon independent narrow qualification.

E. **Tooling and verification reference.**

   Run from the selected worktree root using the repository-compatible Python environment. The inspected pins are Titanoboa 0.2.7 and Vyper 0.4.3. Verify installed versions and compiler settings. A read-only JSON-RPC/Web3 workflow against `https://base-rpc.publicnode.com` worked for the plan review; validate historical-state support and use bounded retries/backoff. Official endpoint throttling and `cast` problems were reported by the reviewer; treat them as environment-specific troubleshooting leads, not universal failures. PublicNode also refused a historical bytecode request with HTTP 403 during re-review, so provider availability is not guaranteed; bounded fallback to `https://mainnet.base.org` is reasonable after checking the same chain/block hash. Never expose credentials or load a signer for measurement.

   Set `RIPE_BOA_CACHE_DIR`, `PYTHONPYCACHEPREFIX`, `XDG_CACHE_HOME`, pytest cache/basetemp, and any Hypothesis storage to writable task-local paths before starting tests. `tests/conftest.py` reads `RIPE_BOA_CACHE_DIR`. `tests/conf_utils.py::clear_transient_storage()` clears transient state, not necessarily EIP-2929 access warmth. Prove real fresh transaction boundaries; use fresh fork environments with identical restored production storage where needed. Do not warm measured paths through setup or treat fresh source deployments with empty history as production-state measurements.

   Fork mechanics verified against the installed Titanoboa 0.2.7 implementation: `boa.fork(url, block_identifier=PIN_NUMBER)` creates/replaces the global environment; supply the block **by keyword as an integer**, never an omitted `safe`/`latest` default or an ambiguously positioned value. In a fresh process, first obtain the header through Web3, verify chain ID/hash against the recorded pin, call `boa.fork`, then assert `boa.env.evm.patch.block_number == PIN_NUMBER` and the matching chain ID/timestamp. Use the retained runtime versions at that pin. Do not import a helper that forks a second environment after deploying fixtures, or use `allow_dirty=True` to conceal discarded setup. Every bisection/sweep trial starts from the same independently verified initial state; fresh process/fork setup does not itself prove that later fixture setup did not warm the measurement. Keep remote historical state caching separate from EVM transaction access warmth. Document the measured transaction-boundary mechanism and control proving it. The installed implementation also uses placeholder prior-block hashes apart from the fetched parent; if a route reads deeper BLOCKHASH history, use a compatible backend or report that limitation.

   Start with `tests/registries/test_price_desk_isolation.py`, `test_price_desk_gas.py`, and `test_price_desk_aggregate_protocol_gas.py`; preserve relevant existing regressions. Add a Base-specific pinned-fork test module and a reproducible read-only qualification runner if the existing harness cannot express this work. Name and document their actual CLI arguments and assertions; do not present a planned command as an already working tool.

   Exercise quote/conversion/ETH helpers, admission, has-feed, and snapshot paths; caller-gas boundaries; healthy sources behind budget-exhausting/malformed/reverting sources; full-ring and nested routes; root/nested classifier edge cases; independent parameter/override changes; authorization while paused and after local-governance relinquishment; bounds/events/readbacks; and rollback of previous snapshot and surrounding user-state writes. Test ordinary non-strict degradation separately from root-underfunding reversion.

   Update old stipend literals without merely comparing measurements to arbitrarily raised settings. Keep independent regression ceilings, route correctness assertions, and documented headroom. Admission success at one warm/current state does not establish cold or future full-ring executability.

   Explicit gas invocation, from the worktree root after configuring caches:

   ```sh
   python -m pytest -q -m gas tests/registries/test_price_desk_gas.py tests/registries/test_price_desk_aggregate_protocol_gas.py
   ```

   Run isolation/parameter regressions separately. `gas`, `artifact` and `fork_qualification` are already registered; apply the marker to a new Base module without changing pytest configuration. Run that actual module explicitly with `-m fork_qualification` and record the block/hash/config. CLI `-m` overrides the configured marker selection while retaining the other defaults; do not clear all addopts for these ordinary explicit lanes. Require nonzero collection. Default collection ignores `tests/deployment`; for a deployment-only command that needs this exclusion removed, use explicit test paths, preserve `-p no:unraisableexception`, and document the replacement addopts/marker filter and collected test count. [Pytest documents last-option precedence](https://docs.pytest.org/en/stable/example/simple.html#how-to-change-command-line-options-defaults). For manifest changes, run:

   ```sh
   python -m pytest -q -m artifact tests/test_current_manifest_consumers.py
   ```

   Keep `test_price_desk_complete_deployed_runtime_is_below_eip170` passing using complete deployed code, including immutables. This test was independently rerun on the master pin during plan review: **1 passed; 17,742 deployed bytes; 6,834 bytes of headroom**. The deployed immutable candidate readback was 17,898 bytes. These indicate room for investigation, not proof that a proposed implementation fits. Recompute final runtime and all touched deployables.

   Preserve and label unrelated baseline failures and any corrective fork fixtures. Do not claim final-stack qualification from a fixture that repairs an unresolved production dependency. Report incomplete tests, deselections, provider failures, and unqualified routes explicitly.

F. **Deliverables and activation handoff.**

   The planning packet and preserved evidence are durable at `docs/chains/base/pricedesk-gas-plan/` in the shared repository; hand over/copy the whole directory, not temporary-file links. `evidence/` contains the earlier live readbacks and superseded v2 planning record; those older instructions are historical, and this prompt controls. Keep live readbacks dated; reverify entry/handoff state. Record a hash inventory so another reviewer can authenticate the packet. In the selected implementation worktree, place the result report at `docs/chains/base/pricedesk-gas/README.md`, contemporaneous decisions at `decision-log.md`, machine-readable block/source/config/gas results at `qualification.json`, and deployment/Safe instructions at `activation-handoff.md`. Put sanitized evidence under that directory's `evidence/`. Record exact branches/commits, dirty-input hashes where applicable, compiler settings, ABI changes, runtime sizes, commands, collection/pass counts, supported limits, and remaining owner decisions. Never overwrite prior rehearsal artifacts.

   If replacing PriceDesk, reconstruct source IDs/order/disabled state, governance, ETH handling, registry settings, budgets/overrides, and token scales. Inventory the union of required current collateral, retained vault holdings, live source assets, and nested dependencies; do not rely solely on an incoming empty/incomplete MissionControl. Maintain disabled slot 3 and the retained Aero policy. `numAddrs` is the exclusive upper bound: `range(1, numAddrs)` with value 10 visits IDs 1–9; value 9 would omit ID 9. Reconstruct through registry methods, never manual counter edits, and assert exact count, every slot and the last enabled source.

   Bootstrap actual ERC-20 scales through the authorized governance/setup path, with exact `10**decimals` readbacks and conversion tests. ETH uses implicit 1e18 and `syncTokenScale(ETH)` reverts. Exclude zero and non-token reference sentinels such as Chainlink BTC; classify NFTs and other non-ERC-20 assets explicitly. Permissionless synchronization performs a full has-feed traversal and only fills unset scales; governance/switchboard bypasses those checks. Do not grant new bootstrap privileges. Unset scales return zero for non-strict conversions and revert for strict ones, even when `getPrice` works.

   Treat manifests as recorded artifacts to reconcile. The plan-review readback found the existing MissionControl entry `0x559E53F42b68b4995732Dba4aF300796761DBC19` matched live slot 5 and had 15,712 bytes of code; do not repeat a blanket dead-address claim. Prepare candidate labels/manifests and tested promotion logic. A deployed but inactive candidate must not replace the canonical active PriceDesk entry. Canonical manifest promotion and its readback/consumer tests belong after separately authorized activation; do not invent live addresses/receipts or preemptively publish a candidate as active.

   Draft the precise slot-7 proposal change and report the other batch calls untouched/unreviewed by this work. Replacing nonce-477 calldata creates a different Safe transaction hash and requires the appropriate fresh signatures; an existing signed proposal cannot simply be edited. If nonce 477 already executed, re-resolve the pending entry/current Safe nonce and prepare the corresponding later action instead of reusing stale calldata.

   The inspected registry's valid re-proposal overwrites an existing pending entry and restarts its delay; cancellation is not mechanically required first. This applies to on-chain registry state, not Safe-service proposal replacement. The new address must already contain contract code and must meet registration-validity checks. Therefore deployment/configuration/readbacks precede proposal. Use the actual `confirmBlock`; 21,600 Base blocks is approximately 12 hours at two seconds per block, not a promised wall-clock schedule. Account for a matured old pending entry until a replacement/cancellation is actually executed.

   Rehearse final deployment/setup and proposal/confirmation on a pinned fork with the final artifacts and intended stack. Include proposal overwrite/delay-reset and permission checks in fork tests. Prepare post-deployment runtime verification, parameters/configuration/scales readbacks, final Safe simulation, and post-activation validation instructions. Verification against the eventual actual live replacement address remains a later gate because live deployment is outside this task.

   Stop only the dependent work and record its blocked/no-go result when source/deployment identity cannot be bound, cold boundaries cannot be demonstrated, the prototype acceptance/bound fails, an out-of-scope source/interface change is needed, the selected route cannot fit the stated headroom/limits, or live proposal/activation drift invalidates that handoff. A pricing-policy alternative is delivered as a proposal only; it does not block unrelated work or authorize its implementation. Continue unaffected analysis and deliver concrete evidence/options; do not weaken invariants or manufacture a pass. No silent halt and no live corrective transactions.

   Completion means the specified implementation and meaningful source/full-transaction evidence pass for each claimed workstream. A larger cap, successful warm price, non-reverting receipt, matching manifest label, or green default suite is insufficient. Keep live deployment, signing, submission, activation, and unrelated upgrade approval explicitly separate.
