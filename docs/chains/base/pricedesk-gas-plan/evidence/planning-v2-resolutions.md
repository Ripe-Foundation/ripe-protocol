All 28 reviewer items have a disposition below. “Addressed” means the planning requirement or factual correction is in `updated-prompt.md`; it does not mean the proposed contract change, gas sweep, or activation is complete.

The revised prompt is self-contained. It no longer refers an implementer to unseen feedback or relies on remembered counts, an unnamed branch, or an unspecified historical block. Its six numbered steps are the references used below.

| Item | Disposition and change | Prompt location |
| --- | --- | --- |
| B1: contradictory root/nested requirements | Accepted the need for an explicit definition; did **not** approve registry membership as proof of nesting. Root underfunding and exhaustion inside a fully supplied parent source budget are separate. Membership authenticates an address, not its position in the current call stack. Source standalone validation and unregistered candidate callbacks are counterexamples to the proposed classification. Added a bounded feasibility prototype, a caller-context matrix, forced full-budget admission, and a concrete scope/decision stop if the guarantee cannot be proved. | 2, 4, 5 |
| B2: static quote path | Accepted. Explicitly prohibits persistent/transient depth writes, write-based locks, and mutable shared accounting on the static quote path. Notes stateful snapshot entry does not lift static restrictions in descendants. EIP-214/EIP-1153 references included. | 2 |
| B3: missing historical block | Included 51,493,600 as the reviewer's **approximate** failure reference, and 51,491,284 as the branch's earlier reported parity case. Requires exact hash/state/artifact binding. Rejected “if it does not reproduce, the block is wrong” as overconfident: routing, gas, warmth, build, provider, or the original finding can also explain a discrepancy. Requires investigation/escalation without prematurely clearing the defect. No claim that the cost crossed the cap between those blocks has been established here. | 3 |
| B4: missing branch/commit | Accepted. Verified PR #231's live GitHub head and base; supplied the full head SHA and exact immutable diff. Named the new narrow and hardening branches and their base/dependency rules. No checkout or branch was changed during this review. | 1 |
| B5: pending Safe/registry hazard | Accepted with material corrections from live reads. At block 51,524,517, slot 7 had **no on-chain pending update**, while Safe nonce 477 was unexecuted and its batch contained the candidate proposal. Included Safe transaction hash, active/candidate addresses, delay, entry/handoff refreshes, and state distinctions. The inspected HQ wrapper requires governance for confirmation; “anyone” is unsupported. Overwrite/reset semantics apply to a valid on-chain re-proposal, not editing a signed Safe transaction. Added fresh-signature and deployment-before-proposal sequencing. | 1, 6 |
| B6: locations/branch/PR/non-goals | Accepted. Named report, decision-log, JSON evidence, activation-handoff and evidence-directory locations. Defined isolated branches and future PR bases. Current implementation handoff finishes locally; no push/PR publication without later instruction. Added an explicit production-file ceiling and owner decision before source/department interface expansion. | 1, 4, 6 |
| S1: recursion topology | Accepted the topology requirement, corrected the facts. The current ten source files are not ten recursively quoting deployed sources. Chainlink/Pyth/Stork contain no direct `PriceDesk.getPrice` calls; Curve has four direct matching calls in the inspected source, not ten. These are source observations, not an exhaustive deployed call-graph audit. Require the actual deployed graph, validation paths, indirect calls, disabled slots, and absent depth guard to be documented. | 3 |
| S2: caller blast radius/reserve | Accepted inventory and recoverable reserve. The 84/22 figure is unverified and is not handed over as fact. A narrow mechanical scan found 40 direct PriceDesk pricing/conversion calls across 16 non-mock files, but excluded alias/helper forms and is deliberately not substituted as the total. Require complete enumeration by method, strictness and user consequence. A reserve, if used, is governance-adjustable with proved lower/upper bounds and liveness tests. | 2, 3, 4, 5 |
| S3: snapshot sweep | Accepted. Added independent 150k/1.5M/2M/3M/6M snapshot sweep, fixed quote-budget control, combined 3M/3M trial, due-state assertions, and final selected pair. | 3 |
| S4: immutable sweep method | Accepted. Compile pinned source once, deploy fresh fork-local desk instances for each constructor pair, and restore identical registry/config/state. Storage-version measurements occur later and explicitly include lookup/guard overhead. | 3 |
| S5: minimum successful gas | Accepted with a precision improvement. Bracket and bisect on restored identical state using expected result/state predicates; probe boundaries and higher limits. Acknowledge non-monotonic gas-dependent fallback behavior instead of claiming bisection alone proves a global minimum. | 3 |
| S6: tooling guidance | Accepted. Provided repo-root execution, pinned Python dependencies, working PublicNode endpoint, credential-free Web3/RPC fallback, bounded retry, exact cache variable `RIPE_BOA_CACHE_DIR`, and writable paths. Official-RPC throttling and `cast` unreliability remain reviewer-reported environment leads, not universal claims. PublicNode worked in this review. | 5 |
| S7: manifest staleness | Accepted live registry authority and manifest lifecycle; rejected the current dead-MissionControl assertion. Readback found the manifest's MC matched live slot 5 and had code. Require reconciliation rather than blind trust. Candidate labels/manifests precede activation; canonical active-address promotion follows authorized activation, with consumer tests. Existing offline manifest tests do not independently establish on-chain freshness. | 1, 6 |
| S8: size headroom | Accepted and independently verified. The actual runtime gas-marked test passed on master: 17,742 bytes, 6,834 headroom. On-chain candidate code was 17,898 bytes. Included both with their distinct identities and retained final-runtime qualification. | 1, 5 |
| S9: reset semantics/risk | Accepted with precision. Reset on count change can reseed a usable fresh PPS; a reset does not by itself imply the entire asset is unpriceable. Historical smoothing is lost and rebuilds over time/observations; a permanently smaller ring can alter the long-run window. Added upside-throttle interaction, valid-snapshot assertion/atomic revert, and timelocked two-step update. Existing underlying-price dependencies still matter. | 4 |
| S10: stop before broad redesign | Accepted. Production changes to source/department interfaces require explicit scope approval; a large callback redesign cannot slip into the Base unblock. Stop the dependent hardening implementation after delivering a concrete prototype/failure/proposal, while completing independent narrow work. | 2, 4, 6 |
| S11: minimize contract changes | Accepted as an explicit working instruction at the beginning and a concrete file ceiling. Did not invent a verbatim earlier user instruction or claim an uninspected policy file mandated it. | Opening, 4 |
| S12: decision record | Accepted. Record config-versus-replacement selection and evidence when chosen, before implementation, at the named decision-log path. Routine technical work proceeds; actual pricing-policy changes await the owner. | 4, 6 |
| N1: setter while paused | Accepted. Use `gov._canGovern`, not `_canPerformAction`; test recovery while paused and after local-governance relinquishment. State the immediate-update policy explicitly. | 4, 5 |
| N2: explicit baseline | Accepted. Names 250k quote / 150k snapshot / 75k feed-check, and distinguishes this master generation from the PR's 1.5M/1.5M candidate. | 1, 3 |
| N3: ETH token-scale exception | Accepted. ETH is implicit 1e18; do not call `syncTokenScale(ETH)`. Also classify zero, BTC reference sentinel, NFTs and non-ERC-20 entries instead of blindly looping the asset union. | 6 |
| N4: bootstrap authorization/gas | Accepted. Explain the permissionless feed-traversal/unset restrictions and existing governance/switchboard bypass. Use the authorized setup path without granting new privileges, then verify exact scales and conversions. | 6 |
| N5: retired stale-time argument | Accepted. Nonzero caller stale-time strict rejection/non-strict zero is intentional and must remain. | 2 |
| N6: fixed 75k feed checks | Accepted for the scoped work. Keep 75k and test caller-underfunding. If actual deployed-source measurements contradict adequacy, report a scope decision rather than silently redesigning the feed-check budget or asserting all future implementations must fit. | 4, 5 |
| N7: snapshot asymmetry | Qualified. Source-level snapshots often do not directly call PriceDesk, unlike quote routes; some are no-ops. But Undy snapshot computation calls an external vault's `convertToAssets`, so a universal no-recursion assertion requires that external graph to be traced. Added the useful asymmetry without assuming the conclusion. | 2, 3 |
| N8: exact test invocation | Accepted. Added explicit `-m gas`, `-m fork_qualification` for the actual Base module, `-m artifact` for manifest tests, and `-o addopts=''` to neutralize default exclusions. Require nonzero collection and actual commands/results. | 5 |
| N9: unseen-feedback reference | Accepted. Removed the instruction about “the feedback's pseudocode.” The replacement states the semantic contract, rejected shortcut, tests, and unresolved feasibility condition directly. | 2 |
| N10: callback routing validity | Accepted and elevated to its own emphasized gate. Some quote functions pass the desk explicitly, other validation paths resolve it from HQ, and canonical-source checks may reject a noncanonical test desk. Requires actual fork-local routing and traces; no claim that every callback always uses HQ. | 3 |

The remaining owner choice is whether measured changes to Undy's averaging history or the winning oracle may be adopted without a separate decision. The recommended rule, used in the updated prompt, preserves current pricing policy and requires approval of a concrete proposed change. This does not hold up measurements or writing the plan. A separate scope decision becomes necessary only if the feasibility work demonstrates that source/department interface changes are needed.

Other explicit defaults in the prompt are local-only commits/publication, separate reviewable workstreams, and immediate governance-only gas/reserve updates that remain available while paused. They are proposed task rules, not claims that the owner has approved a production implementation or deployment.

Verification performed during this planning review:

- Read the complete reviewer attachment, the prior prompt, local PriceDesk/registry/governance/source/test code, the pinned PR diff, the branch's staging/review documentation, bridge migration, and manifest records.
- Read PR #231 via GitHub: open, target `master`, head `bdf7f3da7113aabde60ab8eceab6a960a841bb88`.
- Read Base and the Safe transaction service without signing or broadcasting. `live-summary.json` contains the bounded on-chain readback and all 22 decoded Safe calls; `live-readback.json` retains the service's raw decoded batch. The chain values are pinned to the recorded block; Safe service state was read shortly afterward and is not a block-pinned chain proof. Both role variants of the slot-7 confirm simulation reverted because no update was pending; those simulations **do not independently prove confirmation authorization**. The governance-only statement is supported by the inspected local and manifest-embedded RipeHq source and remains subject to deployed-code binding in the implementation work.
- Independently ran the existing deployed-runtime test: `1 passed in 94.02s`; output `PRICEDESK_DEPLOYED_RUNTIME size=17742 headroom=6834`. The command used `-m gas`, writable task-local Boa/Python/pytest caches, and the master pin. No quote/snapshot sweep or complete Base transaction qualification was run.
- Checked the relevant static-call restrictions against the primary [EIP-214](https://eips.ethereum.org/EIPS/eip-214) and [EIP-1153](https://eips.ethereum.org/EIPS/eip-1153) specifications. No static quote-depth write scheme is proposed.
- Repository status remained `?? docs/research/`, matching the initial unrelated user state. No repository edits, branch changes, commits, pushes, PR changes, live transactions, or automated monitoring were performed. The updated prompt, resolution record, and read-only review evidence were written only under this task's temporary output directory.

Primary code anchors for the important corrections: local `contracts/registries/RipeHq.vy:163` (proposal/confirmation authorization), `contracts/registries/modules/AddressRegistry.vy:244` (overwrite/delay), `contracts/priceSources/UndyVaultPrices.vy:474` (reset condition), `:660` (reset/reseed), `:693` (weighted PPS), `:906` (external vault conversion), `contracts/registries/PriceDesk.vy:235` (static source call), and `tests/conftest.py:10` (Boa cache variable). The updated prompt binds the eventual implementation to the PR generation and calls for live code authentication where those source facts are used operationally.
