# Fourth-review resolutions

All six main findings and six smaller items have a disposition below. The proposed implementation is not implemented or gas-qualified by this documentation task. Two owner decisions remain pending; the fault model and stacking on PR #231 are approved.

| Item | Disposition and resulting instruction |
| --- | --- |
| M1 — Configurability was last | Accepted. Governance defaults and address-keyed overrides are the Stage 1 deliverable, followed by measurements. Constructor immutables are acknowledged as already implemented at the pinned PR head. No preliminary immutable-sweep gate. State drift remains a hypothesis; a later costly quote alone does not establish its cause. |
| M2 — Empty narrow workstream | Accepted the combined-work recommendation, pending the owner's branch answer. One replacement implements both configurable budgets and funding checks. Existing approval to stack on #231 is preserved. Removed the active/config-only/uniform-cap selection ladder and narrow-first sequencing. The old eight-exhausting-source arithmetic does **not** pre-reject uniform sweep values under the newly approved single-source fault model; per-source controls remain required regardless. |
| M3 — Uniform pre-call checks | Accepted as approach one and production-eligible if proved/tested. All frames enforce the same forwarding check; the parent isolates nested failure after supplying its own source budget. Removed the runtime root/nested classifier requirement. Parent budgets must cover the actual graph, not merely exceed individual child values. Keep exact capped forwarding; SafeCall's helper forwards all available gas and is not a literal transplant. Its buffer explicitly depends on its implementation. Added the caught-inner-failure counterexample against relying solely on a post-call threshold; no unsupported claim that OpenZeppelin is vulnerable. |
| M4 — Missing fault model | **Owner approved:** normal ×1.5 plus one faulty source identity across every invocation in a transaction, including nested/repeated quote/feed/snapshot calls. This is stronger than allowing only one exhausted call. Model changed fallback reachability, disjoint nesting, minimum outer funding, actual transaction limits and existing 20% reserve. Preserve multiple-failure correctness tests without pretending those cases are the chosen production liveness promise. |
| M5 — Cross-chain/feed checks | Accepted canonical Base/Robinhood implementation, default preservation at 250k/150k/75k, affected constructor wiring and meaningful requalification. Feed-check configurability is recommended and explicitly pending the owner answer. Fixed 75k release defaults can coexist with governance adjustability. Added feed-check underfunding to D01. |
| M6 — Excess process | Accepted three stages: contract/tests, Base values/monitoring, rehearsal/handoff. Main prompt is approximately one third of the former length, with a separate practical reference. Removed the four-hour cutoff, optional-hardening priority and repeated stop clauses; retain two genuine approaches and scoped evidence-driven decisions. The review's assertion that an agent cannot track hours is unnecessary and not generally true; the cutoff is removed because it added no useful acceptance criterion. |
| N1 — Existing tools | Accepted. Named diagnostic, FullUpdate, source-count guard and Robinhood test paths. Found and documented a material caveat: diagnostic is locked to block 51,312,366, tests only three assets and stages fresh replacements. Extend machinery for the correct pin/inventory/retained source generation rather than blindly running or weakening its guard. |
| N2 — Undy-wide measurements | Accepted attribution to the reviewer and explicit undyUSDC/AERO/USD plus full-inventory coverage. The 1,719,201 figure is no longer called unattributed; its original trace, frame and state are still absent from the packet, so reproduction/provenance is required. We did not rerun these measurements. |
| N3 — Packed storage | Accepted explicit single-word budget packing with masks, bounds, inheritance and update tests. Independently compiled a Vyper 0.4.3 layout probe: a three-uint64 struct occupies three slots; a uint256 occupies one. An override lookup plus inherited defaults may require two reads, so the review's blanket one-SLOAD claim is qualified. No unsupported compiler auto-packing assumption. |
| N4 — Monitoring | Accepted a read-only human/JSON CLI with approximately 70% warning, per-frame evidence and explicit unknown/failure/coverage states. At exactly 1.5× headroom, measured usage is about 66.7%; 70% is an early warning, not assurance against rapid growth. Include dependency/funding requirements, not only whole-quote consumption. No actual automation or external messages are created. |
| N5 — Gas estimation | Accepted state-equivalence testing at real estimate-derived and generous limits from identical pre-state, plus the submitted buffer. Require the same sender/calldata/value/access list and relevant client settings, due snapshot writes and user-state assertions. A manually inferred estimate or non-reverting receipt is insufficient. |
| N6 — Safe nonce 477 | Accepted immediate separate owner work, independent of implementation. Draft precise hold/removal/replacement instructions, refresh current chain/service state, and distinguish proposal, pending registry entry and activation. A replacement Safe payload requires a new hash/signatures; service removal does not invalidate old signatures. No Safe mutation is authorized here. |

## Owner decision record

1. **Approved this revision:** normal ×1.5 plus one faulty source identity on every invocation per transaction.
2. **Pending:** configurable quote/snapshot/feed budgets, with existing Robinhood/local defaults retained. Recommendation: all three.
3. **Previously approved:** stack on PR #231. **Pending this revision:** one combined implementation branch rather than the prior two-branch sequence. Recommendation: combined, still stacked on #231.

No pricing-policy choice is needed for this handoff: history/smoothing/source-order changes remain proposals only. Future source/caller ABI expansion or relaxation of headroom requires a concrete owner decision; routine implementation choices do not.

## Preservation of earlier corrections

Archived v2/v3 prompts and resolution records remain evidence, not executable instructions. This revision deliberately supersedes narrow-first ordering, mandatory uniform sweeps, the root/nested classifier research gate, the four-hour limit, and all-sources-exhaust transaction arithmetic. Subject to the owner answers it also supersedes the fixed-feed-only scope and two-branch layout.

The current prompt/reference retain all other load-bearing corrections: frozen SHA with upstream drift recorded; isolated checkout/fallback; exact registry count and disabled/Aero policy; historical versus fresh pins; correct callback routing; real cold transaction boundaries and Boa environment behavior; strictness and retired stale-time compatibility; snapshot external dependencies and actual state transitions; scope ceilings and pause-safe governance recovery; token-scale bootstrap, ETH and sentinel handling; live HQ authority; full runtime sizing; existing markers and nonzero test collection; authenticated source/build/config identities; distinct Safe/registry lifecycles; final integrated-tree qualification; and draft-only live activation.

Policy-analysis caveat retained: changing Undy snapshot count resets/reseeds its ring and loses prior smoothing, though a valid fresh snapshot can remain usable; other inspected config-field changes preserve its cursor. Any separate policy proposal must quantify averaging window, price discontinuity, upside-throttle/recovery and changed winning oracles. These alternatives are not a prerequisite to the requested gas implementation.

## Verification of this revision

Read the full latest review and current/superseded packet; inspected pinned configuration, diagnostic, FullUpdate references, source-count guard and relevant PriceDesk/tests. Confirmed PR #231 remains open against master at the stated SHA through a read-only GitHub query. Read primary Optimism SafeCall and OpenZeppelin forwarder source and compiled the local Vyper layout probe. The latter verifies storage layout only, not a production guard or packed-budget implementation.

Read-only refresh at finalized Base block 51,531,230 again found the old desk active, no pending slot-7 update and the same governance. The separate Safe-service response reported nonce 477 unexecuted with the same hash. The first sandbox attempt failed name resolution; a permitted PublicNode attempt returned HTTP 403, then the official Base RPC succeeded. Full evidence preserves those distinctions; this was not a new bytecode authentication or gas test.

No contract implementation, gas sweep, Base full-flow qualification, Git branch/commit/publication, Safe mutation or live transaction was performed. Documentation validation covers links, current-versus-archived decisions, feedback coverage and the regenerated packet hash inventory.

Primary references: [Optimism SafeCall](https://github.com/ethereum-optimism/optimism/blob/develop/packages/contracts-bedrock/src/libraries/SafeCall.sol), [OpenZeppelin ERC2771Forwarder](https://github.com/OpenZeppelin/openzeppelin-contracts/blob/master/contracts/metatx/ERC2771Forwarder.sol), [EIP-150](https://eips.ethereum.org/EIPS/eip-150), [EIP-214](https://eips.ethereum.org/EIPS/eip-214), [EIP-1153](https://eips.ethereum.org/EIPS/eip-1153). These support the design constraints, not a claim that the proposed Vyper adaptation has passed.
