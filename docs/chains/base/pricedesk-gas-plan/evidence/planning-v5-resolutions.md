# Fifth-review resolutions

The review's 13 numbered findings and all low-priority items are addressed below, with the two Safe subpoints tracked separately. This is planning work, not production implementation or qualification. The former eager quote design is withdrawn. The owner has answered the earlier scope/branch choices, expanded D01 to Teller, and confirmed margins; the quote/storage design answers remain pending when this draft is written.

| Item | Resolution |
| --- | --- |
| R01 — Eager quote checks and live topology | Accepted the structural counterexample. Independently read priorities `[1,8,2,9,4,5]` at the reviewer's block. A source with allowance B cannot provide B again after nested overhead; an eager revisit can prevent a cheap no-feed result and healthy later fallback. Move topology/dependency binding to Stage 1 and withdraw eager ordinary quotes. Proposed result-dependent proof uses saved **pre-call** gas, accepts only canonical positive/feed or zero/no-feed results without full-allowance proof, and requires proof for every failure/malformed/zero-with-feed result. Feed/snapshot/admission checks stay eager. Actual historical builds and transitive dependencies must be audited/tested; source grep alone does not prove clean answers are gas-independent. The complete six-asset/fallback-loss claim and ~422k Base-client implication are not independently established by this planning pass. |
| R02 — Non-final prompt | Partly resolved by owner answers: configurable feed checks and one combined branch on #231 are now approved. New quote/storage choices are explicitly requested rather than inferred. Final packet must incorporate those answers and remove pending recommendations before labeling itself ready for implementation. A reviewer recommendation is not an owner answer. |
| R03 — Teller's 500k call | Confirmed on both master and pinned PR: `Teller._performHousekeeping` directly calls Curve `addGreenRefPoolSnapshot()` with 500,000 gas and fail-open behavior. **Owner approved D01 coverage for both paths**, with minimal Teller enforcement/tests while preserving genuine failures. This is a distinct initial allowance; making it a fourth adjustable control or aliasing it to another snapshot budget is not automatically necessary/authorized. Measure it and propose a concrete change if insufficient. |
| R04 — Four cases and agreement | Added exact named due/funded, due/underfunded+rollback, not-due/no-op and genuine-failure/isolation tests for each path, plus estimate/state-equivalence coverage. The user/owner agreed the stated policy. D01 closes only after source-bound implementation evidence passes and the owner accepts it following implementation review; no aggregate Boolean or quote-only pass closes it. Legitimate no-op tests are funded: eager checks do not promise an underfunded no-op will succeed. |
| R05 — External consumers | Restored a required frontend/wallet/keeper/Appraiser/on-chain-integrator inventory and endpoint minimum-gas table. Verified local Underscore Appraiser directly calls non-strict price/conversion views and `addPriceSnapshot`; live build must still be bound. Added estimator/submitted buffer/fault-case/batch/compatibility fields and scope separation for external-repo patches. Did not relabel a reported Robinhood limit as Base evidence. |
| R06 — Recovery/triggers | Added symptom-to-action table: caller gas versus source cap, measured override increases, safe rollback of settings, persistent snapshot skips, no-fit configurations and delayed replacement. No assumed rollback to an incompatible old desk. Explicit triggers cover priorities, source/implementation changes, overrides, vault/strategy/dependency changes, asset/ring/batch growth, Base repricing/caps, client estimator changes, compiler/EVM settings and integrated-tree changes. |
| R07 — Governance/floors | Accepted the authority concern, with two qualifications. Constructor floors cannot guarantee a source remains usable after growth, and resetting an override to a floor can still impair it; raising caps can also impair routes. Moreover, a pinned live read found **PriceDesk source-registry delay 0**, not 21,600; that figure is the historical HQ replacement delay. The proposed immediate governance policy is explicit, and constructor-floor/storage choice was sent to the owner. Base floors must be modest baselines, not arbitrary high uniform values that nullify source differentiation. |
| R08 — Plain structs/immutable defaults | Accepted plain member access as the simpler recommendation. Compiler probes independently show one SLOAD for one struct member, two for two members and three for the whole struct. The prior layout proof was correct about slots but insufficient to justify forced packing. Removed packing/mask requirements. Recommended immutable defaults plus per-source overrides/no global setter, subject to owner answer. Zero fallback/reset semantics still exist; they do not disappear merely because defaults become immutable. Exact reviewer microbenchmark gas numbers were not rerun or generalized. |
| R09 — Stacked margins | **Owner explicitly confirmed** source ≥1.5× measured need, normal transaction ≥1.5× consumption and ≤80% of cap, with measured batch limits. Already configured fault envelopes are counted once, not multiplied by 1.5 again. At 16,777,216 the normal-consumption ceiling is 8,947,848, before any larger proof/fault requirement. Preserve the distinction between reducing keeper batches and an indivisible user position that cannot fit. |
| R10 — Early arithmetic | Accepted arithmetic before fork sweeps. Three disjoint 3M quote + 1.5M snapshot pairs yield 13.5M, above 13,421,772 before other work. Qualified the reachability claim: an exhausting feed check may prevent that source's snapshot call. The implementer must prove the actual transaction call tree/failure pattern before treating this as a reachable lower bound; healthy feed checks followed by quote/snapshot failures may expose it. |
| R11 — Monitoring thresholds | Accepted 70% warning and 85% critical, plus nested-budget fit on every run and explicit unknown coverage. Larger headroom may be selected but requires requalification; removed any implication that a larger lazy quote cap has no possible healthy-path effect or failure cost. At 1.5× initial headroom, about 5% growth crosses the first warning intentionally. |
| R12 — Real estimator/environment | Named Anvil, explicit RPC input, loopback, fixed pin, sanitized allowlisted environment, and Stage 1 preflight. Verified installed 1.3.5 and a real synthetic `eth_estimateGas`/snapshot/revert/state-equivalence smoke: 43,106 estimated gas and 100,000 generous gas yielded identical state. Sandbox listener initially failed; a permitted retry passed and shut down. This is not a Base fork or proof of current Base hardfork compatibility. Existing tools' whole-`.env` entry points must be avoided/adapted; loading environment variables is not itself proof that they instantiated a signer. |
| R13 — Robinhood deployment distinction | Accepted. Preserve canonical-source/test compatibility and defaults, while leaving Robinhood live desk and manifest untouched. Verify historical deployed bytecode against its authenticated historical source/build, not newly modified source. New lookup/guard code still requires relevant current-source regression/gas checks. |

| Low-priority item | Resolution |
| --- | --- |
| L01 — Child revert data | Restored an explicit prohibition on classifying funding from callee-controlled revert bytes. The parent proves its own call's funding; test forged/reused error selectors and caught inner failures. |
| L02 — Live HQ / third-party messages | Restored both explicit boundaries: fork-local routing only, never repoint live HQ; no third-party messages. |
| L03 — Fault definition | Defined exhausting-and-failing identity on every invocation. Arbitrary wrong prices/successful lies are not covered by this availability model. Known clean-result gas sensitivity still disqualifies the conditional proof design for that route. |
| L04 — Setter authority | Explicit `gov._canGovern` only; no general Switchboard privilege copied from token-scale sync, no arbitrary protocol role. Preserve paused/HQ-governance recovery tests. A caller who independently is the actual governor is authorized by that role, not by Switchboard membership. |
| L05 — Upper-bound rationale | Finite bounds keep eager calls and failure-path full-funding proofs feasible and arithmetic safe. Qualified the review's universal-bricking claim: under lazy quotes, a clean outcome may still succeed with a large nominal cap. That does not make oversized settings safe. |
| L06 — RPC unavailable | Named concrete deliverables: offline implementation/tests, runner/monitor interface, attempted pin/hash and sanitized errors, cached evidence provenance, explicit unqualified rows, exact resume command and open dependent D01/activation gates. No silent exit or fabricated qualification. |
| L07a — Proposal versus confirmation | Re-emphasized nonce 477 only proposes, valid re-proposal overwrites/resets delay, and later confirm-all payloads are the activation risk. Inspect their targets and actual pending state. |
| L07b — Omitting slot 7 | Corrected operational recommendation: omission and proceeding is not known safe. Prove compatibility of new departments with the retained desk or hold dependent cutover together. Preserve all signature/nonce/dependency checks. |
| L08 — Session folder | Copy by the packet's file inventory, excluding `.claude/` and unlisted files. The reviewer-created empty folder is left untouched; it is not an artifact or implementation input. Hashing/copying must apply the same exclusion. |

## Decision record

- Approved previously: stack on PR #231; normal ×1.5 plus one faulty identity across repeated/nested invocations.
- Approved this revision: configurable feed checks; one combined branch; both PriceDesk and Teller snapshot paths under D01 with minimal Teller changes; retain stacked margins and measure batch limits without re-multiplying fault envelopes.
- Pending: result-dependent ordinary quote checks with eager feed/snapshot/admission and authenticated-source audit.
- Pending: immutable modest constructor defaults/floors, unpacked per-source override structs and no global-default setters.

The floor is an authority tradeoff, not proof of permanent liveness. The lazy quote design is a conditional correctness argument to test, not an EVM guarantee of full funding on every clean call. The main handoff must state both plainly.

## Checks performed and limits

Read all supplied feedback/current packet and relevant pinned D01/Teller/tooling/configuration source. Reverified the v4 hash inventory before archiving its current documents. Read the priority list at the reviewer's historical block; later enumeration hit HTTP 429 and remains incomplete. Separately read PriceDesk delay zero at that same block. Compiled three Vyper member-access probes and ran the synthetic Anvil estimator/state test. Read primary EIP-150/Vyper references and local Anvil CLI documentation. No production contract changes, actual Base fault injection, full source-generation audit, Base estimator qualification, complete caller inventory, Git mutations, live transactions or Safe edits occurred.

All earlier material requirements remain in the current prompt/reference unless explicitly superseded: pinned isolation, cold measurement, real callback routing, scales/sentinels, registry count, source failure versus caller underfunding, final runtime and integrated-build identity, authority, source-bound D01 and separate live activation. The v4 pre-call-every-quote and packed/mutable-default recommendations are historical and must not be executed.

Primary references: [EIP-150 forwarding rules](https://eips.ethereum.org/EIPS/eip-150) and [Vyper 0.4.3 raw_call documentation](https://docs.vyperlang.org/en/v0.4.3/built-in-functions.html#raw-call). They support call mechanics, not qualification of the proposed conditional guard.
