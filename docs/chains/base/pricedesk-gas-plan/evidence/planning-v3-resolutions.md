All **20 numbered findings/nits from the re-review** are addressed: R1–R13 plus the seven unnumbered nits, labeled N1–N7 below. Two additional recommendations—the state-drift hypothesis and proposal-only pricing-policy scope—are recorded after the table. A planning disposition is not a completed implementation or qualification claim.

The current [updated prompt](updated-prompt.md) uses Phases 0–6 for ordering and references A–F for detail. The prior 28-item record is preserved at `evidence/planning-v2-resolutions.md`; the current decisions below supersede its conflicting PR, pytest and policy-gate instructions.

| Item | Resolution | Current location |
| --- | --- | --- |
| R1 — Static root/nested dilemma | Accepted. Explicitly names both unsafe defaults: root can break nested pricing, nested can preserve silent underfunding. Prototype defaults unknown to root and measures any resulting breakage. This is not a production-approved classifier or a claim that breakage is inevitable: differentiated budgets may make some routes fit. A failing compiled acceptance matrix plus a harness-correct trace is the evidence for a necessary interface/scope decision. | Phase 5, B |
| R2 — Relationship to PR #231 | **Owner answered: stack on PR #231.** Narrow PR, if later authorized, targets `codex/base-upgrade-fork-rehearsal`; hardening initially targets the narrow branch. No independent overlapping PR to master, no modification of the coworker's branch, and no publication now. The eventual deployment binds the final reconciled integrated commit/tree, not the original immutable-generation SHA. | A |
| R3 — Arithmetic gate | Accepted the gate, corrected the count and gas accounting. IDs 1–9 include disabled slot 3, so the recorded topology has eight enabled sources: eight 3M envelopes are 24M; seven fully exhausted predecessors are already 21M before a healthy last fallback. The 27M figure used nine enabled sources incorrectly. Nested gas is consumed inside its parent's cap, not added again. Whole-transaction accounting still includes repeated root requests, separate snapshots/feed checks, intrinsic gas, other work and forwarding/reserve requirements. Added an early bound check, 20%-of-limit reserve, maximum-configuration validation, and explicit PASS/REJECT/UNPROVEN dispositions. Uniform 3M cannot promise full exhaustive fallback at the stated limit; normal successful quotes can still be diagnostic measurements. | Phases 1–3, C, D |
| R4 — Executability | Accepted. Added a compact ordered phase table with one output/exit criterion per phase, followed by detailed reference requirements. Named the files and the narrow-before-hardening sequence. Historical/no-go evidence is explicitly distinguished from implementation completion. | Opening and phase table |
| R5 — Happy-path choice/headroom | Accepted. Added a preference order among options meeting identical behavior/gates, a default 1.50× per-source cold minimum-required allowance floor, and a whole-operation bound no greater than floor(0.80 × transaction ceiling). A 2M requirement suggests 3M for that source, not every source. These are explicit engineering acceptance floors, not universal guarantees or owner approval to lower safety criteria. Requalify at the selected configuration and across the supported state envelope; tighten for observed drift. Report an incompatible floor/envelope instead of silently reducing margins. | Phase 3, D |
| R6 — Bounded prototype | Accepted. Maximum two candidate approaches and four active engineering hours, whichever comes first; waits separately reported. First approach is the unknown-to-root baseline. Added ten acceptance rows and a compiled control-flow argument requirement. A deterministic design contradiction closes that approach only after harness correctness is established. At the bound, deliver evidence and a smallest-scope proposal; no unapproved third approach/broad redesign. | Phase 5, B |
| R7 — Workstream order | Accepted. Complete the narrow qualification attempt/result first and prioritize it if only one stream can be completed. A narrow NO-GO/partial disposition can precede hardening investigation; this avoids circularly claiming narrow readiness while D01 depends on unresolved hardening. | Opening, Phases 4–5 |
| R8 — Stakes and D01 | Accepted with attribution. Opening names the concrete undyUSDC symptom, reviewer-reported historical ~$40.6k exposure, and Stage 3 D01. The amount lacks a supplied valuation pin/method and is not advertised as current or a loss estimate. Phase 0 refreshes positions/token units. D01 is source-bound: skipped due snapshots through caller underfunding block Stage 3. Quote-only success cannot clear it. | Opening, Phase 0, A |
| R9 — Moved upstream head | Accepted. Re-read upstream, log drift, and continue against the frozen SHA by default. Do not block current work for a target decision or silently rebase. Require explicit final-tree reconciliation before eventual deployment qualification. | A |
| R10 — Volatile planning artifacts | Accepted and completed. Copied the current packet, both prior readbacks, and historical v2 documents into `docs/chains/base/pricedesk-gas-plan/`. Added full candidate bytecode at the old pinned block and a hash inventory. Explicitly tell the next agent to copy the packet because these uncommitted docs are not in the pinned implementation commit. No reliance on `/private/tmp` remains in current handoff instructions. | README, F, evidence/ |
| R11 — Worktree sandbox failure | Accepted. One error report, then an allowed standalone local clone with independent Git administration; archive/patch delivery if metadata creation is also denied. No permission changes, shared-checkout cleanup, or repeated attempts to evade a denied write. A failed worktree does not justify working in the coworker's dirty checkout. | A |
| R12 — Exclusive registry count | Accepted. States `range(1, numAddrs)` and exact count/last-slot assertions. Counter 10 visits IDs 1–9; counter 9 drops ID 9. Reconstruct through registry operations, not manual storage edits. | C, F |
| R13 — Boa fork mechanics | Accepted and checked against installed 0.2.7 code. `boa.fork(url, block_identifier=PIN_NUMBER)` with an integer keyword in a fresh process; precheck chain/header/hash and verify fork number/chain/time. Warn that fork replaces the global environment, `allow_dirty=True` must not hide discarded fixtures, and external fetch caches are not transaction access warmth. Added clean detached-worktree baseline guidance and the installed placeholder-BLOCKHASH limitation where relevant. | A, E |
| N1 — Clearing addopts | Accepted. Removed `-o addopts=''` from ordinary gas/fork/artifact commands. Locally verified `-m gas` alone selects the exact runtime test. Preserve other defaults; deployment-only exceptions must explicitly retain `-p no:unraisableexception` and document altered collection. | E |
| N2 — Existing markers | Accepted. All three markers already exist; mark the new module without changing pytest configuration. | E |
| N3 — Concrete symptom | Accepted with attribution. Opening says the reported candidate returns zero for undyUSDC where the active desk returns a price, and makes reproducing it the task. This review did not run that price reproduction. | Opening, Phase 1 |
| N4 — Report channel | Accepted. Material live-state changes go immediately to `decision-log.md` and the current task progress message. No third-party message or automatic monitor is created. | Opening, A |
| N5 — Duplicate opening | Accepted. Combined objective/scope/minimal-change guidance into a single opening paragraph; the following short paragraph supplies distinct business/gate context. | Opening |
| N6 — Re-verify dated branch facts | Accepted. Explicit startup refresh with frozen-SHA continuation default. Historical snapshot data remains labeled and entry/handoff live checks remain required. | A |
| N7 — Runtime not independently checkable offline | Addressed with new evidence. Re-fetched bytecode at block 51,524,517 after validating its recorded hash, saved the full 17,898 bytes and Keccak-256 `0xe507e54f66dd4c5a34891563734283f59807ce308640183ca3ebe8a8fbdcc05f`. Independently recomputed size/hash offline. A first provider attempt hit HTTP 403; a bounded retry with official-RPC fallback available succeeded. Size/code hash do not establish source-build equivalence, which remains an implementation gate. | evidence/candidate-runtime.block-51524517.json, E |

The state-drift hypothesis is intentionally retained and made explicit: changes to Underscore vault state may change gas costs, so measure historical/fresh pins and valid stress states. It is not established that such drift caused the reported historical difference. This is the reason a single successful measurement is insufficient.

The pricing-policy recommendation is adopted as a scope rule: changes to averaging history, smoothing or winning oracle are measured and proposed only. The implementation run does not pause waiting for approval to apply them, and it does not implement them. A later owner decision may authorize a separate change. This supersedes the old optional approval question and v2 wording.

The old 28-item record remains covered by the detailed reference. In particular: root/nested guarantees and static restrictions remain; governance recovery setters remain available while paused; feed checks remain 75k; cold measurement, actual snapshot writes, token scales/sentinels, valid no-op/failure handling, live registry authority, Safe/registry state distinctions and draft-only activation remain mandatory. Its old B6 PR-to-master choice is replaced by R2; N8 addopts clearing by N1; and pending pricing-policy implementation by the scope rule above. None of the re-review corrections authorizes deployment or weakens D01.

The only owner choice requested this turn—stacking on PR #231—was answered and incorporated. The numerical headroom floors and prototype bound are explicit conservative planning defaults. Any later need to relax those floors, change pricing policy or expand production source/caller interfaces produces a concrete follow-up decision record; it does not stall independent work or silently change this plan.

Validation for this documentation update: complete reads of the supplied feedback and both v2 documents; inspection of pinned branch D01, bridge inventory and local PriceDesk traversal; installed Boa fork-source inspection; `pytest --collect-only -q -m gas` for the runtime test (`1 test collected`); historical candidate-code RPC fetch plus offline size/hash verification; Markdown/link/hash-inventory checks. No quote/snapshot sweep, new runtime test execution, full Base qualification, contract edits, Git branch/commit/publication, live transaction, or Safe mutation was performed in this turn. The earlier one-test runtime result remains historical evidence, not a new run.
