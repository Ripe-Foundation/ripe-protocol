# PriceDesk configurable budgets and snapshot underfunding

Implement governance-adjustable **per-source quote, snapshot and feed-check budgets**, with correct caller-underfunding handling, in the canonical PriceDesk source. Include the smallest necessary Teller change for its separate Curve reference-pool snapshot call. Deliver focused code/tests, Base qualification, a read-only monitor and a recovery/activation handoff. The reported trigger is zero-priced undyUSDC at the proposed 1.5M desk; reproduce it without assuming state drift caused it. 3M/3M is an experimental heavy-source configuration, not a release value.

**Owner decisions:** one combined branch stacked on PR #231; configurable feed checks; D01 covers both PriceDesk snapshots and Teller's direct Curve snapshot; retain source 1.5× headroom, transaction 1.5× normal headroom and the 80%-of-cap limit, measuring supported batch sizes. The fault model permits **one source identity exhausting its allowance and failing on every invocation** in a transaction, including repeated/nested calls. Wrong prices and arbitrary successful gas-dependent lies are outside that fault model; known misleading clean-result behavior still disqualifies the proposed quote design.

**Two design answers pending:** result-dependent quote checks with eager feed/snapshot/admission checks; and immutable defaults plus unpacked, constructor-floored override structs. The specifications below are the recommended versions. Finalize these answers before issuing this document as executable instructions.

Use isolated branch `codex/pricedesk-configurable-gas` from frozen PR #231 head `bdf7f3da7113aabde60ab8eceab6a960a841bb88`; eventual PR target is `codex/base-upgrade-fork-rehearsal`. Record upstream drift and reconcile the integrated tree before deployment. Copy the packet by its hash inventory, excluding `.claude/`; archived prompts are historical only. Read [implementation-reference.md](planning-v5-reference.md) for exact tooling, tests and operational details.

Scope: PriceDesk, Teller's identified snapshot path, necessary interfaces/configuration/migration consumers and directly related tests/tools/docs. Preserve price arithmetic, freshness, source order, smoothing and genuine-source failure isolation. Source/caller ABI redesign and client-repository edits require a concrete scope decision. Finish locally with reviewable commits/patches. No publication, live deployment, signing, Safe mutation, activation, live-HQ repointing or third-party messages. Pricing-policy alternatives remain proposals only.

## Stage 1 — Topology, feasibility, contract and tests

**Before selecting parameters or implementing guards**, authenticate current and retained source builds, priorities, assets, dependencies and external callers. At reviewer block 51,532,116 the independently read priority list was `[1,8,2,9,4,5]`; slot 8 is the retained Undy route to bind. Reproduce Chainlink failure → Undy underlying lookup → revisit Undy's no-feed slot → healthy underlying fallback. Test each configured Undy asset; do not assume all share USDC or have identical fallback coverage. The former eager quote design is withdrawn: inside allowance B, a revisited source cannot eagerly receive another full B after overhead. Increasing B cannot solve that self-revisit.

Preflight the explicit `BASE_MAINNET_RPC_URL`, pinned historical reads and **Anvil** estimator backend immediately. The reference includes a verified local smoke test, sandbox limitation and Base-fork requirements. Reuse FullUpdate/diagnostic machinery without loading the whole `.env`, reading a production key or creating a live signer. Run the paper fault-envelope arithmetic before costly sweeps. Then implement focused code and tests; provider trouble must not halt offline work.

**Storage recommendation.** Retain public immutable defaults, including a new feed-check default, and use an address-keyed plain struct of three uint256 override fields. Read the needed member directly; do not load the entire struct on quote/feed paths. Zero means use the corresponding immutable; provide reset and effective getters. There is no global-default setter. Preserve existing allowance getter selectors and document that they expose defaults, not overrides.

Nonzero overrides must be at least their constructor defaults and below qualified finite maxima. Constructor defaults/floors are modest chain baselines, not uniform 3M floors; choose Base values from qualification, using heavy-source overrides where needed. Preserve Robinhood/local default values 250k/150k/75k. This design retains fallback/reset rules; immutables do not eliminate those semantics. Validate initialization/updates, independent fields, events, bounds and reset. Overrides attach to addresses, not slots; define removal/replacement/re-registration, code changes and pre-registration qualification.

Setters use **`gov._canGovern` only**—no Switchboard-wide or arbitrary protocol permission—and remain usable while paused and after local governance relinquishment. Updates are immediate within the approved bounds. Floors reduce permitted lowering but do not prevent resetting below a source's later needs, raising a cap until a route no longer fits, or other governance-induced unavailability. Every effective-budget change requires the published configuration checks. Distinguish the actual source-registry delay from HQ's replacement delay.

**Quote recommendation: decide whether funding proof is required from the result, using gas captured before the call.** Preserve exact capped `raw_call` and canonical 64-byte decoding. Record pre-call gas and derive whether the full effective allowance plus continuation reserve was available, accounting for compiled instructions, cold access, memory and EIP-150 rounding. Do not substitute post-call remaining gas or callee-supplied revert bytes for this evidence.

- Canonical `(price > 0, true)` or `(0, false)`: accept without requiring full-allowance proof, provided the authenticated source/dependency audit supports gas-independent correctness of these clean outcomes.
- Revert/OOG, malformed length/boolean, positive price with false feed, or `(0, true)`: require the saved full-funding proof. Without it, revert this desk frame, including non-strict calls. With it, preserve existing failed-source or zero-with-feed semantics and fallback/strictness.
- Feed checks, snapshot calls and **source admission** remain eager: prove full applicable allowance before calling. Admission uses the same effective quote allowance as execution. Unsupported zero/retired-stale-time behavior remains unchanged.

Audit actual old/live source builds **and transitive dependencies**, including proxy implementations, catches/default returns, gas-sensitive branches and gas-dependent routing. Current-source grep is insufficient. Test an adversarial source returning a clean wrong result under low gas to document the conditional design's limitation; do not claim detection it cannot provide. If an admitted production route exhibits that behavior, reject this approach for that route and deliver a concrete alternative. Allow at most one additional in-scope approach after a demonstrated design failure; harness failures do not consume an approach.

A parent isolates a nested failure only after proving its **own** call was fully funded. It never trusts a child's revert data to classify caller underfunding. No root/nested membership classifier or storage/transient depth writes belong on static quote paths. Lazy no-feed visits resolve the eager self-revisit, but actual nested failing/feed-bearing calls still need budgets that fit. Larger budgets are not cost-free: failure envelopes, proof thresholds and dependency behavior change.

**Both snapshot paths are D01 scope.** PriceDesk checks feed and snapshot funding. Teller must distinguish underfunding its separate `addGreenRefPoolSnapshot()` call from genuine Curve failure. Retain 500,000 as that path's initial independent allowance and measure it; do not alias it blindly to per-asset snapshots or add a fourth adjustable control without a justified proposal. Preserve unconfigured/disabled route no-ops, valid not-due returns and the existing genuine-failure event/isolation. Change only the minimal funding enforcement and wiring needed.

Implement named tests for **each** path: `d01_due_funded_writes`, `d01_due_underfunded_reverts_and_rolls_back`, `d01_not_due_is_valid_noop`, `d01_genuine_failure_is_isolated`. Fund legitimate no-op tests adequately; eager protection may reject an underfunded call even if it would have been a cheap no-op. Include prior snapshots/user writes in rollback assertions. The owner has agreed the policy through this decision; D01 exits only after these tests and real estimate/state-equivalence checks pass for both paths, and the owner accepts the source-bound evidence after implementation review. Other Stage 3 gates remain independent.

Preserve isolation/conversion/ETH/strictness regressions, nested and standalone/admission cases, malformed/revert/OOG and caught-inner-failure cases, source lifecycle and setter authorization. Measure complete PriceDesk/Teller deployed runtime. Keep unrelated baseline failures explicit.

## Stage 2 — Base values, complete transactions and consumers

Apply the [retained Pool-1 claim-qualification supplement](../legacy-pool-claim-qualification.md) as a hard release invariant. The LP cohort already has 11 nonzero dust claims, including mcbETH outside the reported defaults inventory and expensive undyAERO. Enumerate full addresses from the pool itself, preserve dust-floor arithmetic, and qualify every actual nonzero balance plus complete legacy flows from cold state within the selected gas/fault envelope. The supplement's historical successful quotes do not satisfy those implementation checks.

Reproduce historical evidence separately from fresh-finalized qualification. Reuse `scripts/diagnose_base_undy_prices.py` and `scripts/base_full_update_fork.py::FullUpdate` with the reference's pin/fresh-source caveats. Cover every supported asset, full Undy inventory, mature rings, supported vault growth, actual callbacks and retained vaults. Attribute the reviewer's 1,719,201 figure until its frame/state provenance is reproduced. Refresh exposure before calling historical ~$40.6k current.

Measure consumed gas and minimum correct caller limits from restored cold states. Check the replacement is used throughout dependencies. Independently vary quote/snapshot/feed budgets around boundaries; 1.5M/2M/3M/6M and the 150k snapshot baseline are diagnostic points, not a required Cartesian sweep. Remeasure final settings and legitimate source-state stress cases; correct price/snapshot state is the success predicate.

Retain the approved margins without multiplying them twice. Effective source budgets cover ≥1.5× measured cold need. Normal transaction allowance covers ≥1.5× measured total gas. For each faulty identity, sum disjoint fully exhausted envelopes **once**, plus 1.5× remaining healthy work and actual forwarding/continuation needs. Allow repeated failure at every invocation; also test selective failures of that same identity when they expose more work. Include new fallback reachability, intrinsic/non-pricing work and Teller's distinct snapshot. Require the maximum normal/fault bound ≤ `floor(0.80*T)`. At recorded Base cap 16,777,216 this is 13,421,772; verify applicable chain/client limits. Normal consumption alone must therefore be ≤8,947,848. Fault envelopes already contain their configured margin; do not multiply them again.

Screen reachable call counts first. Three disjoint Undy quote calls at 3M plus three **reachable** 1.5M snapshot calls total 13.5M before other work and fail this ceiling. If that source also fails feed checks, those snapshots may be skipped; prove the call tree instead of asserting this lower bound for every transaction. Find supported batches and positions from real routes, including deposits/withdrawals, borrowing, valuation, liquidation and deleveraging. A single-position route that cannot fit is a genuine design blocker, not a batch to silently exclude. Validate defaults/overrides and admitted upper-bound configurations under the same model.

Inventory frontend estimation/buffer policy, wallets, keepers, the authorized Underscore Appraiser and other on-chain integrators relying on non-strict views. Deliver `caller-gas-matrix.csv`: endpoint/caller, chain, normal/fault minimum limit, estimator result, submitted buffer/cap, supported assets/batches and compatibility status. Verify actual Base clients; a reported ~422k Robinhood limit is not Base evidence. Prepare concrete client changes if required; do not silently edit other repositories or call an incompatible route ready.

Use pinned Anvil `eth_estimateGas` with identical pre-state/sender/calldata/value/access list and environment. Assert the returned limit and generous limit produce identical due snapshot/user-state changes for both D01 paths; test the submitted buffered limit too. A receipt or Boa gas-used number is insufficient. Anvil must model the applicable Base rules; local smoke success alone is not final-chain proof.

Robinhood scope is source/constructor/test compatibility only. Preserve its defaults, meaningful source-count/topology and aggregate-gas guards. **Do not redeploy its live desk or change its live manifest.** Historical deployed-bytecode verification uses its authenticated historical source/build; new canonical code must not be compared to old live code as if already deployed.

Deliver a read-only monitor with per-source/per-call-kind provenance, effective/requested/forwarded allowance, consumption/minimum-required estimates, **70% warning / 85% critical**, explicit failure/unknown coverage, and nested-route fit checks every run. Whole-quote gas is not source-frame usage; generously fund diagnostic probes so a clipped clean call does not falsely establish headroom. Include Teller's separate path. No scheduler/messages. Larger Undy headroom (such as 2×) requires fault-envelope requalification; monitoring cannot guarantee intervention before a sudden increase.

## Stage 3 — Recovery, rehearsal and handoff

Use the reference's symptom/action recovery table, requalification triggers, exact registry/scales/bootstrap and activation checklist. Setters are the fast recovery path; verify any temporary pause capability before relying on it. Do not assume the old desk is compatible with new departments. Replacing a desk requires its actual HQ proposal/confirmation delay; budget changes are not automatically safe simply because immediate.

Nonce 477 is a separate owner action now. Its slot-7 call only **proposes** the old candidate; the later confirmation is the activation risk. A valid later proposal can overwrite/reset delay. Omitting slot 7 and proceeding is not known safe if new departments depend on the new desk. Reconcile the whole dependency graph and any later confirm-all batch before signing/execution. No live action is authorized here.

Deliver local code/tests and `docs/chains/base/pricedesk-gas/{README.md,decision-log.md,qualification.json,caller-gas-matrix.csv,activation-handoff.md}` plus sanitized evidence and reproducible commands. Rehearse final source/build/configuration and full-stack deployment/setup/proposal/confirmation on a pinned fork. Preserve old artifacts. If RPC is unavailable, deliver offline implementation/results, attempted pins/errors, the exact resumable command and explicit unqualified rows; keep Stage 2/D01/activation open where evidence is missing. A report alone does not complete implementation or establish readiness.
